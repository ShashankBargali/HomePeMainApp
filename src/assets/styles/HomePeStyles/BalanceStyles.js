import { StyleSheet } from 'react-native'

const balsStyles = StyleSheet.create({
    container: {
        width: 200,
        height: 200,
        backgroundColor: '#62bfae',
        borderWidth: 3,
        borderColor: '#008068',
        borderStyle: 'dashed',
        borderRadius: 200,
        alignItems: 'center',
        justifyContent: 'center'
    },
    numTxt: {
        fontSize: 50
    }
})  

export default balsStyles